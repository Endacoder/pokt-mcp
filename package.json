{
  "name": "pokt-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "AI & Agents — MCP integrations for Pocket Network RPC, natural language queries, and wallet connect/send",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @pokt-mcp/pocket-client && npm run build -w @pokt-mcp/nl-rpc && npm run build -w @pokt-mcp/wallet-bridge && npm run build -w @pokt-mcp/server",
    "dev": "npm run dev -w @pokt-mcp/server",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Endacoder/pokt-mcp.git"
  }
}
